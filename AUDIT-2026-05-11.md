# RenDS — Auditoría de coherencia docs ↔ código

**Fecha:** 2026-05-11
**Auditor:** Claude (sesión Renso)
**Branch:** `codex/fix-visual-ci-baselines`
**Alcance:** todo el árbol del paquete `rends/` + el workspace padre `/Users/rensoconese/RenDS/`

Este documento es el resultado de cruzar **toda** la documentación de planning/estado contra el estado real del repo. Sirve como mapa de trabajo: cada hallazgo está cita-evidencia y prioridad. Una vez aplicados los fixes, este documento se puede archivar.

---

## 0 · Resumen ejecutivo

**Diagnóstico:** El código está sano (52 componentes consistentes, contratos colocados, tests verdes). La **documentación de estado** está sistemáticamente atrasada — fue escrita asumiendo "repo aún sin push" cuando hace tiempo que hay 18 commits, 2 tags y CI corriendo.

**Inconsistencias halladas:** 26 (7 críticas, 9 altas, 4 medias, 6 bajas).
**+1 crítico detectado en Fase 9** (post-cut): `themes/appearance.css` y `theme-generator.js` no estaban en `package.json` `files` — fix incluido en `[Unreleased]` post-0.8.3.

**Bloqueador principal:** `STATUS.md`, `ROADMAP.md` y el `[Unreleased]` del CHANGELOG describen un mundo previo al lanzamiento. Cualquier nuevo colaborador (humano o agente) que los lea va a tomar decisiones equivocadas.

---

## 1 · Estado real verificado

### 1.1 Repo

| Métrica | Valor real | Comando de verificación |
|---|---|---|
| Branch actual | `codex/fix-visual-ci-baselines` | `git -C rends status` |
| Commits totales | 18 | `git -C rends log --all --oneline \| wc -l` |
| Tags locales | `v0.7.1`, `v0.8.1` | `git -C rends tag --list` |
| Remoto | `github.com/Rensoconese/ren10.git` | `git -C rends remote -v` |
| Branches dependabot | 5 abiertas en origin | `git -C rends branch -a` |
| Outer (`/Users/rensoconese/RenDS/`) | **NO** es git | `git status` (No commits yet) |

### 1.2 Paquete

| Métrica | Valor real | Fuente |
|---|---|---|
| `package.json` version | `0.8.2` | `rends/package.json:3` |
| README declarado | `Current version: **0.8.2**` | `rends/README.md:7` |
| `ren-design.md` declarado | `v0.8.2` | `rends/ren-design.md:5` |
| CHANGELOG último cut | `[0.8.2] — 2026-04-27` | `rends/CHANGELOG.md:310` |
| CHANGELOG Unreleased | poblado con trabajo post-0.8.2 | `rends/CHANGELOG.md:12-308` |

### 1.3 Componentes (52 verificados)

| Layer | Cantidad | Declarado | Diferencia |
|---|---|---|---|
| Primitives | 18 carpetas en `components/primitives/` | 18 en `ren-design.md:656`, README, AGENTS | ✓ |
| Composites | 26 carpetas en `components/composites/` | 26 declarados | ✓ |
| Patterns | 8 carpetas en `components/patterns/` | 8 declarados | ✓ |
| **Total** | **52** | **52** | ✓ |
| Archivos `component.md`/`pattern.md` | 52 | obligatorio por AGENTS.md | ✓ |

### 1.4 CI/CD

| Archivo | Estado | Notas |
|---|---|---|
| `.github/workflows/ci.yml` | Existe | lint + a11y + components + visual, **solo Chromium** |
| `.github/workflows/release.yml` | Existe | tag → tests → npm publish + GitHub Release |
| `.github/workflows/pages.yml` | **Eliminado** | commit `15d027f chore: remove GitHub Pages workflow` |
| `.github/dependabot.yml` | Existe | |
| `.github/PULL_REQUEST_TEMPLATE.md` | Existe | |
| `.github/ISSUE_TEMPLATE/` | Existe | |

### 1.5 Tests

| Suite | Config | Baselines reales |
|---|---|---|
| a11y | `tests/a11y/playwright.config.cjs` ✓ | — |
| visual | `tests/visual/playwright.config.cjs` ✓ | **312 PNG** en `visual.spec.cjs-snapshots/` |
| components | `tests/components/playwright.config.cjs` ✓ | — |

### 1.6 Higiene del repo (verificación contra ROADMAP Hito 1)

| Item | Realidad |
|---|---|
| `LICENSE` en `rends/` | ✓ existe |
| `README.md` en `rends/` | ✓ existe |
| `CONTRIBUTING.md` en `rends/` | ✓ existe (rends/CONTRIBUTING.md) |
| `CODE_OF_CONDUCT.md` en `rends/` | ✓ existe (rends/CODE_OF_CONDUCT.md) |
| `.github/ISSUE_TEMPLATE/` | ✓ existe |
| `.github/PULL_REQUEST_TEMPLATE.md` | ✓ existe |
| `.github/dependabot.yml` | ✓ existe |

**Solo falta:** LICENSE/README/CONTRIBUTING/COC en el outer (workspace). Pero como el outer **no es el repo público**, posiblemente no aplique.

---

## 2 · Inventario de inconsistencias

Severidad: 🔴 crítico · 🟠 alto · 🟡 medio · 🟢 bajo

### 2.1 🔴 `rends/STATUS.md` describe un mundo previo al primer commit

**ID:** A1
**Archivo:** `rends/STATUS.md`

| Línea | Texto actual | Realidad |
|---|---|---|
| 4 | `Versión en disco: **0.8.1** · Próximo cut: **0.8.2** (Unreleased listo)` | `package.json` = `0.8.2`, ya cortado en CHANGELOG, hay commit `1301c32 chore: release v0.8.2` |
| 11 | `Repo sin commits. Tags v0.7.0 / v0.8.0 / v0.8.1 no existen.` | 18 commits, tags `v0.7.1` y `v0.8.1` existen |
| 11 | "Compare-links del CHANGELOG apuntan al vacío" | Verificable: necesita verificarse el bloque de compare-links del CHANGELOG |
| 15 | `rends/docs/, rends/blocks/, rends/themes/preview.html, rends/create/index.html` | `blocks/` fue renombrado a `templates/` (CHANGELOG:66, 220) |
| 13 | "Sin `.github/workflows/`" | `ci.yml` y `release.yml` existen |

**Acción:** reescribir el documento entero. Probablemente colapse a "casi todo done; solo falta push del branch actual + activar Pages (si se quiere) + decidir si npm publish ya ocurrió o no".

### 2.2 🔴 `rends/ROADMAP.md` checkboxes y referencias desactualizadas

**ID:** A2
**Archivo:** `rends/ROADMAP.md`

| Línea | Texto actual | Realidad |
|---|---|---|
| 4 | `Versión actual en disco: 0.8.1 (release packaging real: pendiente)` | 0.8.2 |
| 28 | `248 PNGs en visual.spec.cjs-snapshots/` | 312 PNGs reales |
| 30–42 | tabla con `❌ Pendiente` para CONTRIBUTING, COC, .github/*, tags, commits | Todos `✓` excepto tags `v0.7.0`/`v0.8.0`/`v0.8.2` |
| 50–57 | Hito 1: checkboxes `[ ]` para LICENSE root, README root, CONTRIBUTING, COC, ISSUE templates, PR template, dependabot | Todos los items con check existen ya (los que tienen "root" son ambiguos: si root = outer, falta; si root = rends/, ya están) |
| 62–66 | Hito 2: `ci.yml`, `release.yml`, `pages.yml` como `[ ]` | `ci.yml` y `release.yml` existen; `pages.yml` fue eliminado deliberadamente |
| 64 | `pages.yml — deploya rends/docs/, rends/blocks/, ...` | doble error: workflow eliminado + blocks renombrado |
| 70–75 | Hito 3: Cut 0.8.2 todo `[ ]` | Ya cortado |
| 79–83 | Hito 4: primer commit, tags retroactivos | Hay commits y dos tags; tags `v0.7.0`/`v0.8.0`/`v0.8.2` retroactivos no existen aún |
| 87–93 | Hito 5: npm publish | Estado real desconocido (verificar con `npm view rends version`) |
| 141 | `F7.27 (último PHASE doc, 23-abr-2026)` | el último commit es de mayo, no abril |

**Acción:** reescribir reflejando: Hitos 1–3 ✓, Hito 4 ~75% (faltan tags retroactivos), Hito 5 dudoso, Hito 6 (F8 = 52 páginas por componente) ✓ done en `[Unreleased]`, Hito 7 sigue como nice-to-have.

### 2.3 🔴 CHANGELOG `[Unreleased]` con secciones duplicadas

**ID:** A3
**Archivo:** `rends/CHANGELOG.md` líneas 12-308

El bloque `[Unreleased]` (entre línea 12 y línea 310 donde empieza `[0.8.2]`) tiene secciones repetidas:

| Sección | Primera aparición | Segunda aparición |
|---|---|---|
| `### Added` | 14 | (única) |
| `### Changed` | 64 | **230** |
| `### Fixed` | 115 | **252** |
| `### Removed` | 218 | **304** |
| `### Security` | — | 306 (vacía) |
| `### Accessibility milestones` | — | 308 (vacía) |

Contenido entre 14-218 y 230-308 tiene **overlap parcial** (ambos mencionan el rename `blocks/→templates/`, p.ej. líneas 66 vs 232). Parece un copy-paste accidental durante un merge.

**Acción:** consolidar a una sola tanda Added/Changed/Fixed/Removed/Security/A11y, deduplicar bullets idénticos, preservar ambos cuando difieren.

### 2.4 🔴 `outer/README.md` desactualizado (referencia `blocks/`)

**ID:** A4
**Archivo:** `/Users/rensoconese/RenDS/README.md:24`

```
| ├── blocks/           ← full-page examples
```

`rends/blocks/` ya no existe; fue renombrado a `templates/`.

**Acción:** decisión del usuario → "consolidar: solo rends/". Reducir `outer/README.md` a un índice mínimo (5–10 líneas apuntando a `rends/`).

### 2.5 🟠 `docs/components/*.html` con nombres desalineados de las carpetas

**ID:** A5

| HTML doc | Carpeta de componente | Nombre canónico (`ren-design.md`) |
|---|---|---|
| `ren-ai-patterns.html` | `patterns/ren-ai/` | `ren-ai` |
| `ren-data-table.html` | `patterns/ren-table/` | `ren-table` |
| `ren-form-validation.html` | `patterns/ren-form/` | `ren-form` |
| `ren-icons.html` | `primitives/ren-icon/` | `ren-icon` |
| `ren-input-otp.html` | `composites/ren-otp/` | `ren-otp` |

Esto rompe el contrato que dice AGENTS.md y SKILL.md ("la doc del componente vive en `docs/components/<name>.html`"). Un agente que busque `ren-icon.html` no lo encuentra.

**Acción:** renombrar los 5 HTML al nombre canónico. Actualizar referencias internas (sidebar `_sidebar.html`, `docs/components.html`, command palette, etc.). El CHANGELOG menciona 139 referencias internas para el rename anterior — espera escala similar.

### 2.6 🟠 Dos archivos HTML huérfanos en `docs/components/`

**ID:** A6 / A7

**ren-switch.html** (265 líneas)
- No tiene carpeta `components/primitives/ren-switch/`
- CSS y selectores ya viven en `components/primitives/ren-checkbox/ren-checkbox.css` (14 menciones de `ren-switch`)
- `ren-checkbox/component.md` documenta los tokens (`--ren-switch-*`, `.ren-switch-track`, etc.)
- **Realidad:** es un componente completo en CSS, solo le falta su carpeta propia

**ren-multi-step-form.html** (134 líneas)
- No tiene carpeta `components/patterns/ren-multi-step-form/`
- `components/patterns/ren-form/pattern.md` ya documenta multi-step: `data-steps="N"`, `[data-active]`, "multi-step support"
- **Realidad:** es una *feature/variant* de `ren-form`, no un pattern independiente

**Decisión del usuario:** "promoverlos a componentes reales".

**Recomendación matizada (post-análisis):**
- **`ren-switch`** → promover a primitive independiente. Tiene API de tokens propia ya. Trabajo:
  1. Crear `components/primitives/ren-switch/`
  2. Mover CSS de switch desde `ren-checkbox.css` (extraer ~14 reglas)
  3. Crear `component.md` con la API que ya está en `ren-checkbox/component.md`
  4. Actualizar `ren-design.md` lista de primitives (18 → 19)
  5. Actualizar `cli/registry.js`
  6. Actualizar README "18 primitives" → "19", "52 components" → "53"
  7. Tests + a11y existentes ya cubren `.ren-switch`; verificar
- **`ren-multi-step-form`** → reconsiderar. Promoverlo crearía un pattern duplicado con `ren-form`. Opciones reales:
  - (a) Borrar `ren-multi-step-form.html` y enlazar desde `ren-form.html` a una sección "Multi-step" (recomendado)
  - (b) Crear `docs/recipes/multi-step-form.html` como receta, fuera del catálogo
  - (c) Si se promueve igualmente: crear `components/patterns/ren-multi-step-form/` que solo enlace a `ren-form` (esto es lo que el usuario pidió pero es weird)

**Pregunta pendiente al usuario:** ¿Mantener decisión "promover ambos" o aceptar el matiz "switch sí, multi-step no"?

### 2.7 🟠 Duplicación `outer/SHIPPING.md` vs `rends/SHIPPING.md`

**ID:** A8

| | Líneas | Estado |
|---|---|---|
| `/SHIPPING.md` (outer) | 176 | Versión vieja: incluye "create empty repo on GitHub", `git config --global`, regenerar package-lock |
| `/rends/SHIPPING.md` | 97 | Versión actual: asume repo creado, working tree = `~/RenDS/rends/`, instructions para push del branch actual |

**Acción:** decisión del usuario → "consolidar: solo rends/". Borrar `outer/SHIPPING.md`. La versión actual en `rends/` ya tiene la nota "this folder IS the repo".

### 2.8 🟠 Backup viejo `outer/AGENTS.md.rtk-backup-20260506`

**ID:** A9
**Archivo:** `/Users/rensoconese/RenDS/AGENTS.md.rtk-backup-20260506`

Backup del 6-may-2026. `outer/AGENTS.md` ya está actualizado (7-may-2026). El backup es residual.

**Acción:** mover a `_archive/` o borrar.

### 2.9 🟠 27 archivos `PHASE-*-COMPLETE.md` en outer + 2 duplicados en rends/

**ID:** A10

```
/Users/rensoconese/RenDS/PHASE-{4,5,6,7,7-1,7-2,...,7-27}-COMPLETE.md   (27 archivos)
/Users/rensoconese/RenDS/rends/PHASE-7-8-COMPLETE.md
/Users/rensoconese/RenDS/rends/PHASE-7-9-COMPLETE.md
```

AGENTS.md (outer) dice "_archive/ es histórico; **never** treat as current spec". Pero los PHASE-*.md viven sueltos en outer y rends/, no en `_archive/`.

**Acción:** mover todos los PHASE-*.md a `outer/_archive/phases/`. Borrar los dos duplicados de rends/ (PHASE-7-8 y PHASE-7-9). Actualizar AGENTS.md si menciona paths.

### 2.10 ⚠️ FALSO POSITIVO — `cli/registry.js` ya está completo

**ID:** A11
**Archivo:** `rends/cli/registry.js`
**Estado:** descartado en Fase 3 (2026-05-11).

El hallazgo original decía que el registry solo tenía 42 de 52 componentes. **Era incorrecto:** el grep usado para contar (`grep -oE "^\s+[a-z][a-z-]*:"`) solo capturaba keys sin comillas. Las entradas con guión (`'alert-dialog'`, `'color-picker'`, `'context-menu'`, `'date-picker'`, `'date-range-picker'`, `'empty-state'`, `'hover-card'`, `'number-field'`, `'scroll-area'`, `'toggle-group'`) van entre comillas en JavaScript y quedaban fuera del conteo.

Verificación programática:
```js
import('./cli/registry.js').then(m => console.log(Object.keys(m.REGISTRY).length));
// → 53 (incluido ren-switch agregado en F2)
```

Bug real **sí** detectado durante la verificación: `components/index.css` no tenía `@import './composites/ren-date-range-picker/ren-date-range-picker.css'` a pesar de que la carpeta y el CSS existen. Arreglado en F3.

**Acción:** ninguna requerida — todo OK.

### 2.11 ✅ RESUELTO — CI matrix Firefox/WebKit agregado

**ID:** A12
**Estado:** resuelto en Fase 8 (2026-05-11).

Se eligió la opción (b): agregar matrix a `ci.yml` con `continue-on-error: true` para los no-Chromium, alineando con lo que SHIPPING.md ya describía. Los tres jobs Playwright (`a11y`, `components`, `visual`) ahora corren `[chromium, firefox, webkit]` en paralelo:

- Chromium **bloquea** failures (gating)
- Firefox + WebKit son **advisory** (`continue-on-error: ${{ matrix.browser != 'chromium' }}`)
- Cache key per browser para evitar colisiones
- Visual baselines siguen siendo Chromium/linux only (los runs de Firefox/WebKit son informativos; los diffs no bloquean)

ROADMAP Hito 2.5 marcado done.

### 2.12 ⚠️ FALSO POSITIVO — `components-showcase.html` está OK

**ID:** A13
**Estado:** descartado en Fase 3.5 (2026-05-11).

El CHANGELOG dice literalmente "Removed the legacy `components-showcase.html` link **from every sidebar's Reference section**". Verificado:
- Per-page sidebars (`docs/components/*.html` y `_sidebar.html`): no tienen el enlace ✓
- Catálogo principal (`docs/components.html`): SÍ tiene 5 enlaces — pero esto es **intencional**, el catálogo lo usa como vista "quick example" y "browse all"

Es decir, el CHANGELOG era preciso (sidebar ≠ catálogo). El archivo de 230 KB sigue cumpliendo función como vista "view all on one page" desde el catálogo.

**Acción:** ninguna requerida.

### 2.13 ✅ RESUELTO — CSS huérfanos en `docs/` borrados

**ID:** A14
**Estado:** resuelto en Fase 3.5 (2026-05-11).

Los dos archivos contenían 14 + 9 = 23 reglas CSS de "utility classes" (`.constraint-*`, `.content-*`) sin ningún consumidor — `grep -rn` no encontró referencias en HTML/JS/MD. Eran dead code residual de iteraciones tempranas.

```
git rm docs/constraint-driven-design.css
git rm docs/content-guidelines.css
```

Estos archivos no estaban en `package.json` `files`, así que su eliminación no afecta el paquete publicable. Solo limpieza del repo.

### 2.14 🟢 `inject_shell_script.py` (outer) sin doc

**ID:** A15
**Archivo:** `/Users/rensoconese/RenDS/inject_shell_script.py`

Script one-shot que inyecta `<script src="../site/shell.js">` en 61 páginas doc. Útil si se regenera el sitio, no se ejecuta en build pipeline.

**Acción:** mover a `rends/scripts/inject_shell_script.py` con README de propósito, o borrar si fue one-shot.

### 2.15 🟢 `rends.skill` (ZIP binario) vs `rends-skill/` (source)

**ID:** A16
**Archivos:**
- `/Users/rensoconese/RenDS/rends.skill` (17 KB ZIP)
- `/Users/rensoconese/RenDS/rends-skill/SKILL.md` (source)

Diff entre el ZIP y el source no verificado. Probablemente el ZIP es un export/distribution; pero no es claro cuál es la fuente de verdad.

**Acción:** documentar relación o borrar el ZIP si está obsoleto.

### 2.16 🟢 `_archive/rends-skill-workspace` en `outer/_archive/`

**ID:** A17

Directorio histórico. AGENTS.md (outer) ya marca `_archive/` como "never current spec". No requiere acción.

### 2.17 🟢 `outer/STATUS.md` / `outer/ROADMAP.md` (verificar duplicación)

**ID:** A18

`diff -q` mostró que:
- `STATUS.md`: solo existe en `rends/` (no en outer)
- `ROADMAP.md`: solo existe en `rends/` (no en outer)

Pero `outer/README.md:41-42` dice: "STATUS.md" y "ROADMAP.md" como si vivieran en outer. → enlaces rotos en outer/README.

**Acción:** parte del fix de A4 (reescribir outer/README como índice mínimo).

### 2.18 🟢 `outer/global.yml`, `outer/globini.hiplc`, `outer/packagetcg.hipnc`, `outer/skeleton.hiplc`

**ID:** A19

Archivos sueltos en outer no relacionados con RenDS (parecen archivos de Houdini SideFX). Convivencia inocua; el outer es un workspace personal.

**Acción:** no aplica al proyecto RenDS, mencionar solo como observación.

---

## 3 · Decisiones del usuario (registradas)

| Pregunta | Respuesta |
|---|---|
| ¿Por dónde arrancar? | **Auditoría completa primero** (este documento) |
| HTMLs huérfanos | **Promoverlos a componentes reales** (con matiz en A6/A7 — ver §2.6) |
| outer vs rends | **Consolidar: solo `rends/`** |

---

## 4 · Plan de acción priorizado

Ordenado por (a) bloqueo de otros fixes, (b) costo, (c) ROI.

### Fase 1 — Verdad sobre el estado (1-2 h)

1. **Reescribir `rends/STATUS.md`** desde cero reflejando estado real (§1.1–1.6).
2. **Reescribir `rends/ROADMAP.md`** actualizando Hitos 1–5 a su estado real; mantener Hito 6 (F8) como ✓ done desde el `[Unreleased]`; Hito 7 igual.
3. **Consolidar CHANGELOG `[Unreleased]`** (A3): deduplicar las dos tandas Changed/Fixed/Removed.
4. **Reducir `outer/README.md`** a índice mínimo apuntando a `rends/`; eliminar referencia a `blocks/`.
5. **Borrar `outer/SHIPPING.md`** (consolidar en rends/SHIPPING.md).
6. **Mover `outer/AGENTS.md.rtk-backup-*`** a `_archive/`.

### Fase 2 — Naming de docs/components/ (2-3 h)

7. **Renombrar 5 HTMLs** al nombre canónico (A5): `ren-ai-patterns → ren-ai`, `ren-data-table → ren-table`, `ren-form-validation → ren-form`, `ren-icons → ren-icon`, `ren-input-otp → ren-otp`. Actualizar referencias internas en `_sidebar.html`, `docs/components.html`, command palette JS si la hay.
8. **Decidir destino de `ren-multi-step-form.html`** (A7 — pregunta pendiente).
9. **Promover `ren-switch` a primitive** (A6): crear carpeta, extraer CSS de `ren-checkbox.css`, crear `component.md`, actualizar `ren-design.md`, `cli/registry.js`, README, AGENTS.md.

### Fase 3 — CLI completo (1 h)

10. **Agregar 10 componentes faltantes** a `cli/registry.js` (A11).

### Fase 4 — Limpieza (1 h)

11. **Mover `PHASE-*-COMPLETE.md`** (29 archivos) a `outer/_archive/phases/`.
12. **Borrar `outer/inject_shell_script.py`** o moverlo a `rends/scripts/`.
13. **Borrar CSS huérfanos** (`docs/constraint-driven-design.css`, `docs/content-guidelines.css`).
14. **Decidir `components-showcase.html`** (A13): borrar o alinear CHANGELOG.
15. **Resolver `rends.skill` ZIP** (A16).

### Fase 5 — CI y tags (opcional, pre-publish)

16. **Decidir CI matrix** (A12): documentar Chromium-only o expandir a Firefox/WebKit.
17. **Tags retroactivos** `v0.7.0`, `v0.8.0`, `v0.8.2` apuntando a sus commits respectivos (si se quiere para CHANGELOG compare-links).
18. **Verificar `npm view rends version`** y decidir si publicar 0.8.2.

### Post-fase — Cortar `0.8.3` (o `0.9.0`)

19. Renombrar `[Unreleased]` deduplicado → `[0.8.3]` (o `[0.9.0]`) en CHANGELOG con fecha de hoy.
20. Bump `package.json` 0.8.2 → 0.8.3.
21. Update `Current version: X.X.X` en `rends/README.md` y `rends/ren-design.md`.
22. Tag y push.

---

## 5 · Mapa de archivos afectados

### Por modificar (escritura)

```
rends/STATUS.md                                  reescribir
rends/ROADMAP.md                                 reescribir
rends/CHANGELOG.md                               consolidar [Unreleased]
rends/README.md                                  verificar (probablemente OK)
rends/ren-design.md                              update si se promueve ren-switch (+1 primitive)
rends/cli/registry.js                            agregar 10 componentes + ren-switch
rends/components/primitives/ren-checkbox/        extraer CSS de switch (post-promoción)
rends/docs/components.html                       actualizar enlaces tras rename
rends/docs/components/_sidebar.html              actualizar enlaces tras rename
rends/docs/components/{5 archivos renombrados}   rename
/README.md (outer)                               reducir a índice mínimo
```

### Por mover

```
/AGENTS.md.rtk-backup-20260506   →   /_archive/AGENTS.md.rtk-backup-20260506
/PHASE-*-COMPLETE.md (27 archivos)   →   /_archive/phases/
rends/PHASE-7-{8,9}-COMPLETE.md       →   /_archive/phases/
/inject_shell_script.py               →   decisión (mover a rends/scripts/ o borrar)
```

### Por crear

```
rends/components/primitives/ren-switch/
  ├── component.md
  ├── ren-switch.css
  └── (sin JS — es CSS-only como checkbox)
```

### Por borrar (sujeto a confirmación)

```
/SHIPPING.md (outer; queda rends/SHIPPING.md)
rends/docs/constraint-driven-design.css
rends/docs/content-guidelines.css
rends/docs/components-showcase.html (sujeto a A13)
rends/docs/components/ren-multi-step-form.html (sujeto a A7)
```

---

## 6 · Riesgos y consideraciones

1. **Rename de los 5 HTML** rompe enlaces externos si alguien linkeó a `ren-icons.html` desde fuera del repo. Mitigación: agregar redirects con `<meta http-equiv="refresh">` durante 1-2 releases.

2. **Promover `ren-switch`** cambia el conteo "18 primitives" → "19" y "52 components" → "53". Hay que buscar todas las referencias a esos números (`grep -rn "18 primitives\|52 components" rends/`) y actualizarlas en sincronía.

3. **Cortar `0.8.3`** requiere decisión sobre si los renames cuentan como breaking (cambio de URLs de docs). Semver dice no (la API del paquete no cambia), pero para los usuarios sí. Documentar el rename como `### Changed` con mención explícita.

4. **Tags retroactivos** (`v0.7.0`, `v0.8.0`, `v0.8.2`) — los commits exactos a los que apuntar dependen de cuál fue el "release commit" de cada versión. Si no está claro, apuntar a HEAD del primer commit con esos contenidos.

5. **`outer/` no es git** — todo lo que se mueva/borre ahí no queda registrado en historia. Considerar si vale la pena git-inicializar `outer/` para historial, o aceptar que es workspace y los cambios son destructivos.

---

## 7 · Checklist accionable

Para que el usuario marque a medida que se hacen los fixes:

- [x] **A1** STATUS.md reescrito (commit eda246b)
- [x] **A2** ROADMAP.md reescrito (commit eda246b)
- [x] **A3** CHANGELOG `[Unreleased]` deduplicado (commit eda246b)
- [x] **A4** outer/README.md reducido a índice mínimo (commit eda246b + cleanup posterior)
- [x] **A5** 5 HTMLs renombrados (ren-ai, ren-table, ren-form, ren-icon, ren-otp) (commit 973e9c3)
- [x] **A6** ren-switch promovido a primitive (commit 973e9c3)
- [x] **A7** ren-multi-step-form: borrado + sección agregada a ren-form.html#multi-step
- [x] **A8** outer/SHIPPING.md borrado
- [x] **A9** outer/AGENTS.md.rtk-backup-* archivado (ya estaba limpio)
- [x] **A10** PHASE-*-COMPLETE.md (24 outer + 2 rends duplicados) movidos a _archive/phases/
- [x] **A11** cli/registry.js verificado completo (53 entradas; el "incompleto" original era falso positivo del grep)
- [x] **A12** CI matrix: Firefox/WebKit agregados (advisory; Chromium gating)
- [x] **A13** components-showcase.html: mantenido (falso positivo, el "link removed" era literal — del sidebar, no del catálogo)
- [x] **A14** CSS huérfanos de docs/ borrados (constraint-driven-design.css, content-guidelines.css)
- [x] **A15** inject_shell_script.py: archivado en outer/_archive/scripts/
- [x] **A16** rends.skill ZIP: archivado en outer/_archive/ (source en outer/rends-skill/ es la verdad)
- [x] Post — cortar 0.8.3 (commit 0e2b303)
- [ ] Post — tags retroactivos (requiere máquina)
- [ ] Post — npm publish (lo dispara release.yml al pushear tag v0.8.3)

---

## 8 · Cosas que NO están rotas (para no tocar)

Validado durante la auditoría:

- ✓ `ren-design.md` — lista los 52 componentes correctamente, versión 0.8.2 coincide
- ✓ `AGENTS.md` (outer) — coherente con realidad, paths existen
- ✓ `rends-skill/SKILL.md` — alineado con AGENTS.md
- ✓ Loading order documentado (ren-design.md → tokens.md → layouts.md → primitive-zero.md → components.md → component.md/pattern.md)
- ✓ Contratos requeridos: `tokens/tokens.md`, `base/layouts.md`, `base/primitive-zero.md`, `components/components.md` — todos existen
- ✓ 52 archivos `component.md`/`pattern.md` colocados
- ✓ Token system 3-layer (`tokens/primitives/`, `tokens/semantic/`, `tokens/component/`)
- ✓ Tests configs presentes y enlazados desde `package.json`
- ✓ `release.yml` correctamente configurado para tag → npm publish
- ✓ `dependabot.yml` activo
- ✓ Issue/PR templates presentes
- ✓ LICENSE en `rends/`
- ✓ Stylelint config y `lint-tokens.mjs` enlazados

---

*Fin de la auditoría. Lista para discusión y priorización.*
