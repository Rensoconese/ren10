# Header23 Implementation Packet

Implement only `templates/blocks/hero-centered-copy-dual-cta.html` and
`tests/components/header23-header.spec.cjs`.

Build one content-height centered Ren10 hero using native section/h1/p/anchors,
`ren-center`, `ren-stack`, `ren-cluster`, and `.ren-btn`. It owns exactly two
CTA anchors with distinct real local destinations and no form or media. Preserve
one centered finite measure at every width; the cluster must wrap safely on mobile.
Use tokens only, no raw flex/grid skeleton, no script, no framework, and no extra
affordance. First record a real failing focused Playwright RED caused by the absent
HTML. Then implement and prove light/dark, 320 through 1280 seams, focus, 44px,
destinations, no-JavaScript, reduced motion, Axe AA, visual captures, lint, workflow,
agent checks, and diff scope.
