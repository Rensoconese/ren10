# Header27 Implementation Packet

Implement only the declared Header27 HTML and focused spec. TDD RED first. Build
one centered email hero followed by one owned landscape image. Reuse robust native
form/accessibility behavior, not the Header20 split. Exact counts, 639/640 seam,
real no-JS action/terms, nonduplicated validation/success, themes, reduced motion,
focus/touch, axe, captures, lint and workflow must pass.
