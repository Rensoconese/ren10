# Header26 Implementation Packet

Implement `templates/blocks/hero-centered-copy-dual-cta-landscape-image.html` and `tests/components/header26-header.spec.cjs` only after a real RED.

Preserve the authenticated anatomy exactly: one content-height centered section; one copy group with one h1, one description, and exactly two CTA anchors; then exactly one full-width landscape image. The order never changes. Add no form, legal copy, video, dialog, overlay, navigation, logo, or JavaScript.

Use documented Ren10 center, stack, cluster, frame, and button APIs. Both CTAs need distinct resolvable local destinations, safe wrapping at 320px, 44px touch geometry, and visible focus. The image must be an owned intrinsic asset with truthful non-empty alternative text. Verify responsive geometry, light/dark, reduced motion, no-JavaScript, and axe AA. Do not modify catalog, inventory, shared components, tokens, or unrelated modules.
