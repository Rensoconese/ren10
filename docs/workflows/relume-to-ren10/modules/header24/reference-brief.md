# Reference Brief — Header 24

## Retrieval metadata

- Family: `headers`
- Module ID: `header24`
- Retrieved through: authenticated Relume MCP complete source
- Retrieved at: 2026-07-15
- Source variant: content-height centered email capture hero

## Retrieved facts

- One content-height section and constrained outer container.
- One finite-width centered copy region owns exactly one `h1`, one description,
  one email form, and one legal/terms line.
- The form owns exactly one email input and one submit control.
- The form is one column initially and becomes flexible-input plus intrinsic-submit
  in one row from the small breakpoint.
- There is no image, video, dialog, overlay, navigation, logo, second form,
  second CTA, icon, or duplicated responsive tree.

## Responsive and interaction states

- Mobile: heading, description, email, submit, and legal line remain centered;
  input and submit stack.
- From 640px: input and submit share one row; the hero remains one centered column.
- Source submit only prevents navigation and logs state. Ren10 requires a real
  native action plus enhanced visible validation and polite success feedback.

## Visual relationships

- Copy and form share one centered axis.
- The form is narrower than or equal to the copy measure.
- Section and copy-to-form spacing increase through responsive bands.
- No viewport-height forcing.

## Unavailable evidence

- MCP omitted rendered preview and resolved theme values; exact pixels are unavailable.

## Public-output exclusions

- Source copy/classes/URLs/framework code/dependencies/injected HTML
- Fragment-only terms, console-only submission, placeholder-only labelling
- Media, dialog, nav, logo, second form/CTA, primitive palette tokens, raw layout skeletons
