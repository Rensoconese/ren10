# Header24 Implementation Packet

Allowed production/test scope is only `templates/blocks/hero-centered-email-capture.html`
and `tests/components/header24-header.spec.cjs`. Implement one centered content-height
hero with one h1, description, labelled email form, submit, legal/terms and polite
status. Stack form below 640px and switch to field/submit row at 640px. Native action
and terms must resolve without JavaScript. Use one root-scoped ES module only for
enhanced valid success. Protect pristine error from the ren-field display cascade.
No media, extra CTA/form, framework, raw layout skeleton, or forbidden tokens.
Record genuine RED before HTML, then focused GREEN, captures and repository checks.
