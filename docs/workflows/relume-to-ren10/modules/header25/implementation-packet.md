# Header25 Implementation Packet

Implement `templates/blocks/hero-centered-search.html` and `tests/components/header25-header.spec.cjs` only after real RED.

Preserve the authenticated anatomy: one content-height centered/max-width hero with exactly one h1, one description, and one native GET search form containing one visible labelled search input, one decorative leading icon, one submit, and one non-action status. The form is inline where it fits and stacks without overflow at narrow widths. Provide native required validation, a resolvable local result target, no-JavaScript navigation, enhanced polite feedback, light/dark AA, 44px targets, and safe composition.

Use vanilla Light DOM and documented Ren10 APIs. Inline behavior must be `type="module"`, select the Header25 root once, and scope all descendant queries. Do not add legal copy, media, a second form/control, CTA links, catalog entries, inventory changes, shared component changes, or unrelated module edits.
