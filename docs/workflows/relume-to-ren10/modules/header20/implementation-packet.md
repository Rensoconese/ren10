# Header20 Implementation Packet

Implement only `templates/blocks/hero-email-copy-image-left.html` and `tests/components/header20-header.spec.cjs` after real RED. Preserve the authenticated anatomy: content-height container; one-column copy then one image through 1023px; two centered columns at 1024px with image left/copy right; h1, description, exactly one native email form/submit/legal/terms; stacked form then row at 640px; one owned meaningful intrinsic rounded cover image.

Correct the source with visible label, native validation, visible error/invalid state, live status, real GET action and terms destination, no-JavaScript usability, reduced motion, light/dark AA, focus, 44px targets, and no overflow at 320/390/640/1023/1024/1280. Use vanilla Light DOM, tokens, Ren10 layouts/components, and one root-scoped inline `type="module"`. Do not add CTA/dialog/video/overlay/nav/logo, duplicate DOM, protected output, framework code, catalog, inventory, core token, or component changes.
