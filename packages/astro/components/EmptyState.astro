---
import 'ren10/components/patterns/ren-empty-state/ren-empty-state.css';

/**
 * EmptyState — generated from RenDS patterns/ren-empty-state.
 * Contract: ren10/components/patterns/ren-empty-state/pattern.md
 * Registry key: empty-state
 */
export interface Props {
  class?: string;
  [attribute: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={['ren-empty-state', className]} {...rest}><slot /></div>
