---
import 'ren10/components/primitives/ren-progress/ren-progress.css';

/**
 * Progress — generated from RenDS primitives/ren-progress.
 * Contract: ren10/components/primitives/ren-progress/component.md
 * Registry key: progress
 */
export interface Props {
  class?: string;
  [attribute: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={['ren-progress', className]} {...rest}><slot /></div>
