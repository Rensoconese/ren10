---
import 'ren10/components/composites/ren-collapsible/ren-collapsible.css';

/**
 * Collapsible — generated from RenDS composites/ren-collapsible.
 * Contract: ren10/components/composites/ren-collapsible/component.md
 * Registry key: collapsible
 */
export interface Props {
  class?: string;
  [attribute: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<details class:list={['ren-collapsible', className]} {...rest}><slot /></details>
