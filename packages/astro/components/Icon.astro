---
import 'ren10/components/primitives/ren-icon/ren-icon.css';

/**
 * Icon — generated from RenDS primitives/ren-icon.
 * Contract: ren10/components/primitives/ren-icon/component.md
 * Registry key: icon
 */
export interface Props {
  class?: string;
  [attribute: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<ren-icon class:list={['ren-icon-sm', className]} {...rest}><slot /></ren-icon>
