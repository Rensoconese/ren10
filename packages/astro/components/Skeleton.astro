---
import 'ren10/components/primitives/ren-skeleton/ren-skeleton.css';

/**
 * Skeleton — generated from RenDS primitives/ren-skeleton.
 * Contract: ren10/components/primitives/ren-skeleton/component.md
 * Registry key: skeleton
 */
export interface Props {
  class?: string;
  [attribute: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={['ren-skeleton ren-skeleton-text', className]} {...rest}><slot /></div>
