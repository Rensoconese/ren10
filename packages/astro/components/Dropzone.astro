---
import 'ren10/components/composites/ren-dropzone/ren-dropzone.css';

/**
 * Dropzone — generated from RenDS composites/ren-dropzone.
 * Contract: ren10/components/composites/ren-dropzone/component.md
 * Registry key: dropzone
 */
export interface Props {
  class?: string;
  [attribute: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={['ren-dropzone', className]} {...rest}><slot /></div>

<script>
  import { initDropZone } from 'ren10/components/composites/ren-dropzone/ren-dropzone.js';
  document.querySelectorAll('div').forEach((element) => initDropZone(element));
</script>
